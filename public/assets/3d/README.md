# 3D assets

Served from `/public/assets/3d` (mirrored under `/assets/3d`).

| File | Notes |
|------|--------|
| `spiderman.glb` | **Not in git** (~121MB, over GitHub limit). Place locally to enable the hero. |
| `spiderweb.glb` | Web background mesh |
| `hero-character.glb` | Legacy crochet character |
| `crochet-hook.glb` | Legacy hook |

Copy `spiderman.glb` into both `assets/3d/` and `public/assets/3d/` after clone.
